# Basic tools needed by all machines
apt update
# apt upgrade -y

apt install -y curl wget
apt install -y gpg apt-transport-https ca-certificates

# apt install -y tcpdump sysstat nginx
# apt install -y neovim
# apt install -y sd ripgrep fd-find
# apt install -y git
# apt install -y make zip unzip gzip
